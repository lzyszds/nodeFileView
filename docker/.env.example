# 复制为 docker/.env 后，再 docker compose up
# 或启动时临时传入：BASIC_AUTH_PASS='你的密码' docker compose -f docker/docker-compose.yml up -d

BASIC_AUTH_ENABLED=true
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=change-me-strong-password
